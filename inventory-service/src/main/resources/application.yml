server:
  port: 8081

grpc:
  server:
    port: 50052

spring:
  datasource:
    url: jdbc:mysql://smartorder-inventory-db.cfu8kk0wqp6h.eu-north-1.rds.amazonaws.com:3306/inventorydb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always


inventory:
  grpc:
    # SmartOrder DNS çözümlemesi için tam domain adı
    host: inventory-service.smartorder.local
    port: 50052

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: INFO
    com.smartorder: DEBUG
